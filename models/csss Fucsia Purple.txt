/* ================================
   RAFFLE – Fuchsia Purple Neon Theme
================================ */
@import url('https://fonts.googleapis.com/css2?family=Bungee&family=Baloo+2:wght@400;600;700;800&display=swap');

:root {
  --raffle-main: #ff2fcf;
  --raffle-strong: #7b2cff;
  --raffle-soft: #ffb6f2;
  --raffle-light: #ffffff;
  --raffle-gold: #ffd966;

  --raffle-border: rgba(255, 255, 255, 0.34);
  --raffle-glow: rgba(255, 47, 207, 0.55);

  --txt: #ffffff;
  --stroke: #b00086;
  --shadow: #4f168f;
}

* {
  box-sizing: border-box;
}

body {
  margin: 0;
  background: transparent;
  font-family: 'Baloo 2', system-ui, sans-serif;
  color: var(--txt);
  overflow: hidden;
}

.raffle-widget {
  position: relative;
  width: 560px;
  min-height: 430px;
  padding: 14px 16px 16px;
  border-radius: 18px;
  overflow: hidden;

  background:
    radial-gradient(circle at 14% 18%, rgba(255,255,255,.26), transparent 18%),
    radial-gradient(circle at 80% 20%, rgba(255,255,255,.14), transparent 22%),
    linear-gradient(135deg, #ff2fcf 0%, #d52eff 32%, #9b2dff 68%, #5d20d6 100%);

  border: 1px solid var(--raffle-border);

  box-shadow:
    0 10px 28px rgba(90, 25, 180, .30),
    0 0 22px rgba(255, 47, 207, .28),
    inset 0 1px 0 rgba(255,255,255,.35);

  transition:
    opacity 0.25s ease,
    transform 0.25s ease;
}

.raffle-widget.hidden {
  opacity: 0;
  transform: translateY(10px) scale(0.96);
  pointer-events: none;
}

.raffle-widget:not(.hidden) {
  opacity: 1;
  transform: translateY(0) scale(1);
}

.raffle-widget::before {
  content: "";
  position: absolute;
  inset: 0;
  border-radius: inherit;
  padding: 1px;

  background:
    linear-gradient(
      130deg,
      rgba(255,255,255,.90),
      rgba(255,140,238,.74),
      rgba(255,255,255,.50),
      rgba(123,44,255,.56)
    );

  -webkit-mask:
    linear-gradient(#000 0 0) content-box,
    linear-gradient(#000 0 0);
  -webkit-mask-composite: xor;
          mask-composite: exclude;

  pointer-events: none;
  z-index: 3;
}

.raffle-widget::after {
  content: "✦   ✦   ✦   ✦";
  position: absolute;
  inset: 0;
  display: flex;
  justify-content: center;
  align-items: flex-start;
  padding-top: 8px;
  font-size: 13px;
  letter-spacing: 13px;
  color: rgba(255,255,255,.30);
  text-shadow: 0 0 8px rgba(255,255,255,.26);
  pointer-events: none;
  z-index: 1;
}

.raffle-header {
  position: relative;
  z-index: 5;
  text-align: center;
  margin-bottom: 12px;
}

.raffle-header::before {
  content: "";
  position: absolute;
  inset: -260px -260px;

  background: linear-gradient(
    120deg,
    transparent 0%,
    rgba(255,255,255,.08) 38%,
    rgba(255,255,255,.36) 50%,
    rgba(255,255,255,.08) 62%,
    transparent 100%
  );

  transform: translateX(-120%) rotate(12deg);
  animation: raffleFuchsiaShine 4.8s linear infinite;
  pointer-events: none;
  z-index: -1;
  mix-blend-mode: screen;
}

@keyframes raffleFuchsiaShine {
  0% { transform: translateX(-120%) rotate(12deg); }
  45% { transform: translateX(120%) rotate(12deg); }
  100% { transform: translateX(120%) rotate(12deg); }
}

.raffle-badge {
  display: inline-block;
  padding: 3px 12px;
  border-radius: 999px;

  background:
    linear-gradient(90deg, #ff2fcf, #d52eff, #7b2cff);

  font-family: 'Bungee', system-ui, sans-serif;
  font-size: 13px;
  letter-spacing: 1px;
  color: #ffffff;
  border: 1px solid rgba(255,255,255,.32);

  text-shadow:
    -1px -1px 0 var(--stroke),
     1px -1px 0 var(--stroke),
    -1px  1px 0 var(--stroke),
     1px  1px 0 var(--stroke),
     2px  2px 0 var(--shadow),
     0 0 8px rgba(255,255,255,.16);

  box-shadow:
    0 0 14px rgba(255,47,207,.38),
    inset 0 1px 0 rgba(255,255,255,.28);
}

.raffle-title {
  margin: 8px 0 0;
  font-family: 'Bungee', system-ui, sans-serif;
  font-size: 21px;
  line-height: 1.1;
  color: #ffffff;

  text-shadow:
    -1px -1px 0 var(--stroke),
     1px -1px 0 var(--stroke),
    -1px  1px 0 var(--stroke),
     1px  1px 0 var(--stroke),
     2px  2px 0 var(--shadow),
     0 0 10px rgba(255,255,255,.18);
}

.raffle-names {
  position: relative;
  z-index: 5;
  display: grid;
  grid-template-columns: 1fr;
  gap: 7px;
  width: 100%;
  overflow: visible;
}

.raffle-names .raffle-name-item:nth-child(n + 6) {
  display: none;
}

.raffle-name-item {
  position: relative;
  display: flex;
  justify-content: space-between;
  align-items: center;
  gap: 10px;

  min-height: 48px;
  padding: 7px 10px;
  border-radius: 12px;
  overflow: hidden;

  background:
    radial-gradient(circle at 18% 30%, rgba(255,255,255,.20), transparent 14%),
    linear-gradient(180deg, rgba(255,255,255,.18), rgba(255,255,255,.05)),
    linear-gradient(135deg, rgba(255,255,255,.20), rgba(155,45,255,.22));

  border: 1px solid rgba(255,255,255,.22);

  box-shadow:
    inset 0 1px 0 rgba(255,255,255,.26),
    0 6px 16px rgba(90,25,180,.20);

  transition:
    transform 0.12s ease,
    border-color 0.12s ease,
    box-shadow 0.12s ease,
    background 0.12s ease;
}

.raffle-name-item::before {
  content: "";
  position: absolute;
  inset: -45%;
  background: linear-gradient(
    120deg,
    transparent 0%,
    rgba(255,255,255,.08) 38%,
    rgba(255,255,255,.30) 50%,
    rgba(255,255,255,.08) 62%,
    transparent 100%
  );
  transform: translateX(-120%) rotate(12deg);
  animation: raffleNameFuchsiaShine 4.4s linear infinite;
  pointer-events: none;
  z-index: 1;
  mix-blend-mode: screen;
}

.raffle-name-item::after {
  content: "✦";
  position: absolute;
  top: 50%;
  left: 10px;
  transform: translateY(-50%);
  font-size: 14px;
  color: #ffffff;
  text-shadow:
    1px 1px 0 var(--shadow),
    0 0 8px rgba(255,255,255,.28);
  z-index: 2;
  pointer-events: none;
}

@keyframes raffleNameFuchsiaShine {
  0% { transform: translateX(-120%) rotate(12deg); }
  45% { transform: translateX(120%) rotate(12deg); }
  100% { transform: translateX(120%) rotate(12deg); }
}

.raffle-name-item.active {
  transform: scale(1.025);
  border-color: #ffffff;

  background:
    radial-gradient(circle at 18% 30%, rgba(255,255,255,.28), transparent 14%),
    linear-gradient(90deg, rgba(255,255,255,.25), rgba(255,47,207,.36));

  box-shadow:
    inset 0 1px 0 rgba(255,255,255,.36),
    0 0 18px rgba(255,255,255,.34),
    0 0 24px rgba(255,47,207,.38);
}

.raffle-name {
  position: relative;
  z-index: 3;
  flex: 1 1 auto;
  min-width: 0;
  padding-left: 18px;

  font-size: 18px;
  font-weight: 800;
  line-height: 1.05;
  color: #ffffff;

  text-shadow:
    -1px -1px 0 var(--stroke),
     1px -1px 0 var(--stroke),
    -1px  1px 0 var(--stroke),
     1px  1px 0 var(--stroke),
     2px  2px 0 var(--shadow);

  white-space: normal;
  overflow-wrap: anywhere;
  word-break: break-word;
}

.raffle-name-item:nth-child(n + 4) .raffle-name {
  font-size: 16px;
}

.raffle-tickets {
  position: relative;
  z-index: 3;
  flex: 0 0 auto;

  font-size: 13px;
  font-weight: 800;
  color: #ffffff;
  white-space: nowrap;

  padding: 3px 8px;
  border-radius: 999px;

  background: rgba(80, 22, 143, 0.35);
  border: 1px solid rgba(255,255,255,.28);

  text-shadow:
    -1px -1px 0 var(--stroke),
     1px -1px 0 var(--stroke),
    -1px  1px 0 var(--stroke),
     1px  1px 0 var(--stroke);
}

.raffle-winner-screen {
  position: absolute;
  inset: 0;
  z-index: 20;

  display: flex;
  flex-direction: column;
  justify-content: center;
  align-items: center;

  padding: 24px;

  background:
    radial-gradient(
      circle at center,
      rgba(255,47,207,.44),
      rgba(123,44,255,.38) 40%,
      rgba(0,0,0,.86) 76%
    ),
    rgba(0,0,0,.86);

  opacity: 0;
  transform: scale(0.9);

  transition:
    opacity 0.25s ease,
    transform 0.25s ease;
}

.raffle-winner-screen.show {
  opacity: 1;
  transform: scale(1);
}

.raffle-winner-screen.hidden {
  opacity: 0;
  pointer-events: none;
}

.raffle-winner-screen::after {
  content: "";
  position: absolute;
  inset: -45%;
  background: linear-gradient(
    120deg,
    transparent 0%,
    rgba(255,255,255,.08) 38%,
    rgba(255,255,255,.36) 50%,
    rgba(255,255,255,.08) 62%,
    transparent 100%
  );
  transform: translateX(-120%) rotate(12deg);
  animation: raffleWinnerFuchsiaShine 4.4s linear infinite;
  pointer-events: none;
  z-index: 1;
  mix-blend-mode: screen;
}

@keyframes raffleWinnerFuchsiaShine {
  0% { transform: translateX(-120%) rotate(12deg); }
  45% { transform: translateX(120%) rotate(12deg); }
  100% { transform: translateX(120%) rotate(12deg); }
}

.winner-label {
  position: relative;
  z-index: 3;

  font-family: 'Bungee', system-ui, sans-serif;
  font-size: 30px;
  line-height: 1;
  color: #ffffff;
  margin-bottom: 10px;

  text-shadow:
    -1px -1px 0 var(--stroke),
     1px -1px 0 var(--stroke),
    -1px  1px 0 var(--stroke),
     1px  1px 0 var(--stroke),
     2px  2px 0 var(--shadow),
     0 0 12px rgba(255,255,255,.20);
}

.winner-name {
  position: relative;
  z-index: 3;

  max-width: 96%;
  font-family: 'Bungee', system-ui, sans-serif;
  font-size: 46px;
  line-height: 1.08;

  color: #ffffff;
  text-align: center;
  overflow-wrap: anywhere;
  word-break: break-word;

  text-shadow:
    -1px -1px 0 var(--stroke),
     1px -1px 0 var(--stroke),
    -1px  1px 0 var(--stroke),
     1px  1px 0 var(--stroke),
     2px  2px 0 var(--shadow),
     0 0 16px rgba(255,255,255,.22);

  animation: winnerPop 0.7s ease infinite alternate;
}

@keyframes winnerPop {
  from { transform: scale(1); }
  to { transform: scale(1.06); }
}

@media (max-width: 520px) {
  .raffle-widget {
    width: 96vw;
    min-height: 420px;
    padding: 12px;
  }

  .raffle-title { font-size: 18px; }

  .raffle-name-item {
    min-height: 46px;
    padding: 7px 9px;
  }

  .raffle-name { font-size: 16px; }

  .raffle-name-item:nth-child(n + 4) .raffle-name {
    font-size: 15px;
  }

  .raffle-tickets {
    font-size: 12px;
    padding: 2px 7px;
  }

  .winner-label { font-size: 25px; }

  .winner-name { font-size: 36px; }
}