/* ==========================================
   MVP – Glossy Soft Pink Theme
========================================== */

@import url('https://fonts.googleapis.com/css2?family=Bebas+Neue&family=Barlow+Condensed:wght@500;600;700;800&display=swap');

:root{
  --white:#ffffff;
  --pink-light:#ffd7e5;
  --pink-soft:#ffb7cf;
  --pink:#ff7fac;
  --pink-medium:#e74d83;
  --pink-dark:#9e244f;
  --black:#050505;
}

*{
  box-sizing:border-box;
}

body{
  margin:0;
  background:transparent;
  color:var(--white);
  font-family:'Barlow Condensed', Arial, sans-serif;
}

#container{
  position:relative;
  width:430px;
  padding:14px 15px 16px;
  border-radius:16px;
  overflow:hidden;

  background:
    radial-gradient(
      circle at 18% 12%,
      rgba(255,255,255,.72),
      transparent 28%
    ),
    radial-gradient(
      circle at 80% 82%,
      rgba(255,74,135,.35),
      transparent 34%
    ),
    linear-gradient(
      145deg,
      #ffe0eb 0%,
      #ffb6ce 30%,
      #ff8eb5 60%,
      #dc4b7c 82%,
      #8f2047 100%
    );

  border:2px solid #111111;

  box-shadow:
    0 0 0 2px rgba(255,255,255,.60),
    0 0 0 4px rgba(255,111,163,.55),
    0 0 16px rgba(255,87,145,.45),
    0 10px 28px rgba(0,0,0,.42),
    inset 0 2px 0 rgba(255,255,255,.72),
    inset 0 -14px 28px rgba(153,25,73,.24);
}

/* Marco interior */
#container::before{
  content:"";
  position:absolute;
  inset:5px;
  border-radius:11px;

  border:1px solid rgba(255,255,255,.58);

  box-shadow:
    inset 0 0 0 1px rgba(166,36,82,.26),
    0 0 8px rgba(255,255,255,.30);

  pointer-events:none;
  z-index:6;
}

/* Brillo animado */
#container::after{
  content:"";
  position:absolute;
  top:-70%;
  left:-65%;
  width:44%;
  height:245%;

  background:
    linear-gradient(
      110deg,
      transparent,
      rgba(255,255,255,.08),
      rgba(255,255,255,.58),
      rgba(255,255,255,.08),
      transparent
    );

  transform:rotate(17deg);
  animation:mvpPinkShine 5.8s linear infinite;
  pointer-events:none;
  z-index:2;
}

@keyframes mvpPinkShine{
  0%{
    left:-65%;
  }

  45%{
    left:135%;
  }

  100%{
    left:135%;
  }
}

.title{
  position:relative;
  z-index:7;
  margin:0 0 13px;
  text-align:center;

  font-family:'Bebas Neue', sans-serif;
  font-size:30px;
  font-weight:400;
  line-height:1;
  letter-spacing:.055em;
  text-transform:uppercase;
  color:#ffffff;

  -webkit-text-stroke:1px var(--pink-dark);

  text-shadow:
    -2px -2px 0 var(--pink-dark),
     2px -2px 0 var(--pink-dark),
    -2px  2px 0 var(--pink-dark),
     2px  2px 0 var(--pink-dark),
     0 0 3px #ffffff,
     0 0 8px rgba(255,255,255,.82),
     0 0 13px rgba(255,71,134,.72),
     3px 4px 0 #101010;
}

#top-list{
  position:relative;
  z-index:7;
  display:flex;
  flex-direction:column;
  gap:10px;
}

.tip{
  position:relative;
  display:flex;
  align-items:center;
  justify-content:space-between;
  gap:10px;

  min-height:53px;
  padding:9px 13px 9px 17px;
  border-radius:11px;
  overflow:hidden;

  background:
    linear-gradient(
      180deg,
      rgba(255,255,255,.55),
      rgba(255,255,255,.10) 43%,
      rgba(146,25,69,.12)
    ),
    linear-gradient(
      100deg,
      #ffd8e5 0%,
      #ff9dbe 52%,
      #e14b7e 100%
    );

  border:2px solid rgba(78,11,36,.88);

  box-shadow:
    0 0 0 1px rgba(255,255,255,.52),
    inset 0 2px 0 rgba(255,255,255,.62),
    inset 0 -8px 18px rgba(146,25,69,.20),
    0 5px 14px rgba(91,8,39,.27);
}

.tip::before{
  content:"";
  position:absolute;
  left:0;
  top:0;
  bottom:0;
  width:6px;

  background:
    linear-gradient(
      180deg,
      #ffffff,
      #ff84ae,
      #b6295b
    );

  box-shadow:
    0 0 8px rgba(255,255,255,.62);

  z-index:2;
}

.tip::after{
  content:"";
  position:absolute;
  top:0;
  left:-90%;
  width:55%;
  height:100%;

  background:
    linear-gradient(
      105deg,
      transparent,
      rgba(255,255,255,.12),
      rgba(255,255,255,.58),
      transparent
    );

  transform:skewX(-18deg);
  animation:mvpRowPinkShine 5s ease-in-out infinite;
  pointer-events:none;
  z-index:1;
}

@keyframes mvpRowPinkShine{
  0%{
    left:-90%;
  }

  42%{
    left:135%;
  }

  100%{
    left:135%;
  }
}

.tip .name{
  position:relative;
  z-index:3;
  flex:1;
  min-width:0;

  font-size:19px;
  font-weight:800;
  line-height:1;
  color:#ffffff;

  white-space:nowrap;
  overflow:hidden;
  text-overflow:ellipsis;
}

.tip .amount{
  position:relative;
  z-index:3;

  font-family:'Bebas Neue', sans-serif;
  font-size:22px;
  line-height:1;
  letter-spacing:.04em;
  color:#ffffff;
  white-space:nowrap;
}

.tip .name,
.tip .amount{
  -webkit-text-stroke:.7px var(--pink-dark);

  text-shadow:
    -1px -1px 0 var(--pink-dark),
     1px -1px 0 var(--pink-dark),
    -1px  1px 0 var(--pink-dark),
     1px  1px 0 var(--pink-dark),
     0 0 4px rgba(255,255,255,.90),
     2px 3px 0 #111111;
}

/* Primer lugar */
.tip.mvp{
  background:
    linear-gradient(
      180deg,
      rgba(255,255,255,.64),
      rgba(255,255,255,.14) 43%,
      rgba(156,28,75,.15)
    ),
    linear-gradient(
      100deg,
      #ffe8f0 0%,
      #ffabc7 48%,
      #f45c90 100%
    );

  border-color:#5c0d2c;

  box-shadow:
    0 0 0 1px rgba(255,255,255,.68),
    0 0 16px rgba(255,66,131,.48),
    inset 0 2px 0 rgba(255,255,255,.75),
    0 6px 17px rgba(87,7,36,.30);
}

.tip.mvp::before{
  width:8px;

  background:
    linear-gradient(
      180deg,
      #ffffff,
      #ffc1d6,
      #ff4f89
    );
}

/* Segundo lugar */
.tip.second{
  background:
    linear-gradient(
      180deg,
      rgba(255,255,255,.52),
      rgba(255,255,255,.10) 43%,
      rgba(143,25,68,.13)
    ),
    linear-gradient(
      100deg,
      #ffd5e3,
      #ff92b5,
      #d93e73
    );
}

/* Tercer lugar */
.tip.third{
  background:
    linear-gradient(
      180deg,
      rgba(255,255,255,.44),
      rgba(255,255,255,.08) 43%,
      rgba(130,21,60,.16)
    ),
    linear-gradient(
      100deg,
      #ffc8dc,
      #ed78a0,
      #bb2d61
    );
}

.rank-icon{
  position:relative;
  z-index:3;
  margin-right:5px;
  font-size:18px;
  color:#ffffff;

  filter:
    drop-shadow(1px 1px 0 #8e2149)
    drop-shadow(-1px -1px 0 #8e2149)
    drop-shadow(2px 3px 0 #111111)
    drop-shadow(0 0 6px rgba(255,255,255,.80));
}



/* ==========================================
   TIMER – Glossy Soft Pink Theme
========================================== */

@import url('https://fonts.googleapis.com/css2?family=Bebas+Neue&family=Barlow+Condensed:wght@500;600;700;800&display=swap');

:root{
  --white:#ffffff;
  --pink-light:#ffd9e6;
  --pink-soft:#ffadc8;
  --pink:#ff79a5;
  --pink-medium:#dd4779;
  --pink-dark:#9b234d;
  --black:#080808;
}

*{
  box-sizing:border-box;
}

body{
  margin:0;
  background:transparent;
  color:var(--white);
  font-family:'Barlow Condensed', Arial, sans-serif;
}

.timer-widget{
  position:relative;
  width:280px;
  padding:15px 15px 17px;
  border-radius:16px;
  overflow:hidden;

  background:
    radial-gradient(
      circle at 18% 12%,
      rgba(255,255,255,.72),
      transparent 28%
    ),
    radial-gradient(
      circle at 80% 82%,
      rgba(255,66,130,.32),
      transparent 33%
    ),
    linear-gradient(
      145deg,
      #ffe3ed 0%,
      #ffb6ce 32%,
      #ff8db3 60%,
      #dc4779 82%,
      #922047 100%
    );

  border:2px solid #111111;

  box-shadow:
    0 0 0 2px rgba(255,255,255,.58),
    0 0 0 4px rgba(255,108,158,.46),
    0 0 15px rgba(255,74,137,.42),
    0 9px 25px rgba(0,0,0,.40),
    inset 0 2px 0 rgba(255,255,255,.72),
    inset 0 -13px 25px rgba(130,18,59,.22);
}

.timer-widget::before{
  content:"";
  position:absolute;
  inset:5px;
  border-radius:11px;

  border:1px solid rgba(255,255,255,.57);

  box-shadow:
    inset 0 0 0 1px rgba(156,35,79,.24);

  pointer-events:none;
  z-index:5;
}

.timer-widget::after{
  content:"";
  position:absolute;
  top:-70%;
  left:-70%;
  width:48%;
  height:240%;

  background:
    linear-gradient(
      110deg,
      transparent,
      rgba(255,255,255,.10),
      rgba(255,255,255,.60),
      rgba(255,255,255,.10),
      transparent
    );

  transform:rotate(17deg);
  animation:timerSoftPinkShine 5.2s linear infinite;
  pointer-events:none;
  z-index:2;
}

@keyframes timerSoftPinkShine{
  0%{
    left:-70%;
  }

  45%{
    left:140%;
  }

  100%{
    left:140%;
  }
}

.timer-title{
  position:relative;
  z-index:6;
  margin-bottom:11px;
  text-align:center;

  font-family:'Bebas Neue', sans-serif;
  font-size:26px;
  line-height:1;
  letter-spacing:.06em;
  text-transform:uppercase;
  color:#ffffff;

  -webkit-text-stroke:1px var(--pink-dark);

  text-shadow:
    -2px -2px 0 var(--pink-dark),
     2px -2px 0 var(--pink-dark),
    -2px  2px 0 var(--pink-dark),
     2px  2px 0 var(--pink-dark),
     0 0 4px #ffffff,
     0 0 9px rgba(255,255,255,.82),
     0 0 13px rgba(255,62,127,.62),
     3px 4px 0 #101010;
}

.timer-display{
  position:relative;
  z-index:6;
  display:flex;
  justify-content:center;
}

.timer-box{
  position:relative;
  width:182px;
  height:76px;
  border-radius:10px;
  overflow:hidden;

  display:flex;
  align-items:center;
  justify-content:center;

  background:
    linear-gradient(
      180deg,
      rgba(255,255,255,.60),
      rgba(255,255,255,.10) 42%,
      rgba(139,22,64,.16)
    ),
    linear-gradient(
      100deg,
      #ffdce8 0%,
      #ff9cbd 52%,
      #de4b7d 100%
    );

  border:2px solid #111111;

  box-shadow:
    0 0 0 1px rgba(255,255,255,.62),
    0 0 13px rgba(255,63,127,.38),
    0 6px 17px rgba(0,0,0,.38),
    inset 0 2px 0 rgba(255,255,255,.74),
    inset 0 -9px 18px rgba(135,19,61,.18);
}

.timer-box::before{
  content:"";
  position:absolute;
  left:0;
  top:0;
  bottom:0;
  width:7px;

  background:
    linear-gradient(
      180deg,
      #ffffff,
      #ffabc8,
      #d54275
    );

  box-shadow:
    0 0 8px rgba(255,255,255,.72);

  z-index:2;
}

.timer-box::after{
  content:"";
  position:absolute;
  top:0;
  left:-100%;
  width:60%;
  height:100%;

  background:
    linear-gradient(
      105deg,
      transparent,
      rgba(255,255,255,.14),
      rgba(255,255,255,.62),
      transparent
    );

  transform:skewX(-18deg);
  animation:timerBoxSoftPinkShine 4.5s linear infinite;
  pointer-events:none;
  z-index:1;
}

@keyframes timerBoxSoftPinkShine{
  0%{
    left:-100%;
  }

  42%{
    left:145%;
  }

  100%{
    left:145%;
  }
}

#timer-value{
  position:relative;
  z-index:3;

  font-family:'Bebas Neue', sans-serif;
  font-size:40px;
  line-height:1;
  letter-spacing:.07em;
  color:#ffffff;

  -webkit-text-stroke:1.2px var(--pink-dark);

  text-shadow:
    -2px -2px 0 var(--pink-dark),
     2px -2px 0 var(--pink-dark),
    -2px  2px 0 var(--pink-dark),
     2px  2px 0 var(--pink-dark),
     0 0 4px #ffffff,
     0 0 9px rgba(255,255,255,.88),
     0 0 13px rgba(255,61,126,.70),
     3px 4px 0 #111111;
}

.timer-box.timer-running{
  animation:timerSoftPinkPulse 1.15s ease-in-out infinite alternate;
}

@keyframes timerSoftPinkPulse{
  from{
    transform:scale(1);

    box-shadow:
      0 0 0 1px rgba(255,255,255,.58),
      0 0 12px rgba(255,63,127,.30),
      0 6px 17px rgba(0,0,0,.36),
      inset 0 2px 0 rgba(255,255,255,.70);
  }

  to{
    transform:scale(1.025);

    box-shadow:
      0 0 0 1px rgba(255,255,255,.82),
      0 0 22px rgba(255,62,127,.58),
      0 8px 20px rgba(0,0,0,.42),
      inset 0 2px 0 rgba(255,255,255,.86);
  }
}

.timer-box.timer-finished{
  filter:brightness(1.12);

  box-shadow:
    0 0 0 1px rgba(255,255,255,.88),
    0 0 29px rgba(255,50,120,.68),
    0 8px 22px rgba(0,0,0,.45),
    inset 0 2px 0 rgba(255,255,255,.90);
}



/* ==========================================
   GOALBAR – Glossy Soft Pink Theme
========================================== */

@import url('https://fonts.googleapis.com/css2?family=Bebas+Neue&family=Barlow+Condensed:wght@500;600;700;800&display=swap');

:root{
  --white:#ffffff;
  --pink-light:#ffe0eb;
  --pink-soft:#ffb0ca;
  --pink:#ff79a5;
  --pink-medium:#df477b;
  --pink-dark:#9b234d;
  --black:#090909;
}

*{
  box-sizing:border-box;
}

body{
  margin:0;
  background:transparent;
  color:var(--white);
  font-family:'Barlow Condensed', Arial, sans-serif;
}

#goal-wrapper{
  position:relative;
  width:68px;
  height:420px;
  overflow:hidden;
  border-radius:17px;

  background:
    radial-gradient(
      circle at 38% 10%,
      rgba(255,255,255,.72),
      transparent 25%
    ),
    radial-gradient(
      circle at 60% 86%,
      rgba(255,63,127,.32),
      transparent 30%
    ),
    linear-gradient(
      180deg,
      #ffe2ed 0%,
      #ffb3cc 30%,
      #ff87ae 58%,
      #dc4779 80%,
      #902047 100%
    );

  border:2px solid #111111;

  box-shadow:
    0 0 0 2px rgba(255,255,255,.60),
    0 0 0 4px rgba(255,105,157,.46),
    0 0 15px rgba(255,70,135,.42),
    0 10px 27px rgba(0,0,0,.40),
    inset 0 2px 0 rgba(255,255,255,.74),
    inset 0 -15px 25px rgba(126,17,57,.22);
}

#goal-wrapper::before{
  content:"";
  position:absolute;
  inset:5px;
  border-radius:11px;

  border:1px solid rgba(255,255,255,.60);

  box-shadow:
    inset 0 0 0 1px rgba(150,31,73,.22);

  pointer-events:none;
  z-index:5;
}

#goal-wrapper::after{
  content:"";
  position:absolute;
  left:0;
  top:110%;
  width:100%;
  height:35%;

  background:
    linear-gradient(
      180deg,
      transparent,
      rgba(255,255,255,.12),
      rgba(255,255,255,.62),
      rgba(255,255,255,.12),
      transparent
    );

  animation:goalSoftPinkShine 5s linear infinite;
  pointer-events:none;
  z-index:4;
}

@keyframes goalSoftPinkShine{
  0%{
    top:110%;
  }

  45%{
    top:-45%;
  }

  100%{
    top:-45%;
  }
}

#bar-fill{
  position:absolute;
  bottom:0;
  left:0;
  width:100%;
  height:0%;
  z-index:2;
  overflow:hidden;
  border-radius:0 0 15px 15px;

  background:
    linear-gradient(
      90deg,
      rgba(255,255,255,.55),
      rgba(255,255,255,.10) 38%,
      transparent 65%
    ),
    linear-gradient(
      180deg,
      #fff0f5 0%,
      #ffafca 24%,
      #ff6d9e 52%,
      #d63f73 76%,
      #8e2047 100%
    );

  border-top:2px solid rgba(94,13,43,.82);

  box-shadow:
    inset 0 2px 0 rgba(255,255,255,.78),
    inset 0 0 18px rgba(255,255,255,.18),
    0 0 14px rgba(255,61,126,.58),
    0 0 25px rgba(255,61,126,.30);
}

#bar-fill::before{
  content:"";
  position:absolute;
  inset:-30%;

  background:
    linear-gradient(
      180deg,
      transparent,
      rgba(255,255,255,.12),
      rgba(255,255,255,.68),
      rgba(255,255,255,.12),
      transparent
    );

  transform:translateY(125%);
  animation:goalFillSoftPinkShine 4.3s linear infinite;
  pointer-events:none;
}

#bar-fill::after{
  content:"♥";
  position:absolute;
  left:50%;
  bottom:10px;
  transform:translateX(-50%);

  font-size:12px;
  color:#ffffff;

  -webkit-text-stroke:1px var(--pink-dark);

  text-shadow:
    -1px -1px 0 var(--pink-dark),
     1px -1px 0 var(--pink-dark),
    -1px  1px 0 var(--pink-dark),
     1px  1px 0 var(--pink-dark),
     0 0 7px rgba(255,255,255,.92),
     2px 3px 0 #111111;

  animation:goalPinkHeartRise 3.6s linear infinite;
}

@keyframes goalFillSoftPinkShine{
  0%{
    transform:translateY(125%);
  }

  45%{
    transform:translateY(-125%);
  }

  100%{
    transform:translateY(-125%);
  }
}

@keyframes goalPinkHeartRise{
  0%{
    transform:translate(-50%, 15px) scale(.85);
    opacity:0;
  }

  25%{
    opacity:1;
  }

  70%{
    transform:translate(-50%, -65px) scale(1.08);
    opacity:.78;
  }

  100%{
    transform:translate(-50%, -115px) scale(.90);
    opacity:0;
  }
}

.goal-label{
  position:absolute;
  inset:0;
  display:flex;
  justify-content:center;
  align-items:center;
  pointer-events:none;
  z-index:7;
}

.goal-title{
  position:absolute;
  top:50%;
  left:50%;

  transform:
    translate(-50%, -50%)
    rotate(-90deg);

  transform-origin:center;

  font-family:'Bebas Neue', sans-serif;
  font-size:20px;
  line-height:1;
  letter-spacing:.07em;
  text-transform:uppercase;
  color:#ffffff;
  white-space:nowrap;

  -webkit-text-stroke:1px var(--pink-dark);

  text-shadow:
    -2px -2px 0 var(--pink-dark),
     2px -2px 0 var(--pink-dark),
    -2px  2px 0 var(--pink-dark),
     2px  2px 0 var(--pink-dark),
     0 0 4px #ffffff,
     0 0 9px rgba(255,255,255,.86),
     0 0 13px rgba(255,55,123,.65),
     3px 4px 0 #111111;
}

.goal-amount{
  position:absolute;
  bottom:43px;
  left:50%;

  transform:
    translateX(-50%)
    rotate(-90deg);

  transform-origin:center;

  font-family:'Bebas Neue', sans-serif;
  font-size:15px;
  line-height:1;
  letter-spacing:.05em;
  color:#ffffff;
  white-space:nowrap;

  padding:4px 8px;
  border-radius:7px;

  background:
    linear-gradient(
      90deg,
      #ffd9e6,
      #ff9ebd,
      #dc4678
    );

  border:2px solid #111111;

  box-shadow:
    0 0 0 1px rgba(255,255,255,.60),
    0 0 9px rgba(255,59,125,.38);

  -webkit-text-stroke:.7px var(--pink-dark);

  text-shadow:
    -1px -1px 0 var(--pink-dark),
     1px -1px 0 var(--pink-dark),
    -1px  1px 0 var(--pink-dark),
     1px  1px 0 var(--pink-dark),
     0 0 4px rgba(255,255,255,.82),
     2px 3px 0 #111111;
}

.goal-title.neon{
  animation:goalSoftPinkPulse 1.25s ease-in-out infinite alternate;
}

@keyframes goalSoftPinkPulse{
  from{
    filter:brightness(1);
  }

  to{
    filter:
      brightness(1.16)
      drop-shadow(0 0 8px rgba(255,255,255,.72))
      drop-shadow(0 0 11px rgba(255,48,117,.58));
  }
}


/* ==========================================
   RAFFLE – Glossy Soft Pink Theme
========================================== */

@import url('https://fonts.googleapis.com/css2?family=Bebas+Neue&family=Barlow+Condensed:wght@500;600;700;800&display=swap');

:root{
  --white:#ffffff;
  --pink-light:#ffe0eb;
  --pink-soft:#ffb1ca;
  --pink:#ff79a5;
  --pink-medium:#df477b;
  --pink-dark:#9b234d;
  --black:#090909;
}

*{
  box-sizing:border-box;
}

body{
  margin:0;
  background:transparent;
  color:var(--white);
  font-family:'Barlow Condensed', Arial, sans-serif;
  overflow:hidden;
}

.raffle-widget{
  position:relative;
  width:560px;
  min-height:430px;
  padding:15px 17px 17px;
  border-radius:17px;
  overflow:hidden;

  background:
    radial-gradient(
      circle at 18% 12%,
      rgba(255,255,255,.74),
      transparent 27%
    ),
    radial-gradient(
      circle at 80% 82%,
      rgba(255,64,129,.32),
      transparent 33%
    ),
    linear-gradient(
      140deg,
      #ffe2ed 0%,
      #ffb5cd 30%,
      #ff8caf 59%,
      #dc4679 81%,
      #902047 100%
    );

  border:2px solid #111111;

  box-shadow:
    0 0 0 2px rgba(255,255,255,.60),
    0 0 0 4px rgba(255,107,158,.48),
    0 0 17px rgba(255,71,136,.45),
    0 11px 29px rgba(0,0,0,.42),
    inset 0 2px 0 rgba(255,255,255,.74),
    inset 0 -16px 28px rgba(126,17,57,.22);

  transition:
    opacity .28s ease,
    transform .28s ease;
}

.raffle-widget.hidden{
  opacity:0;
  transform:translateY(10px) scale(.96);
  pointer-events:none;
}

.raffle-widget:not(.hidden){
  opacity:1;
  transform:translateY(0) scale(1);
}

.raffle-widget::before{
  content:"";
  position:absolute;
  inset:5px;
  border-radius:11px;

  border:1px solid rgba(255,255,255,.60);

  box-shadow:
    inset 0 0 0 1px rgba(153,32,74,.24);

  pointer-events:none;
  z-index:6;
}

.raffle-widget::after{
  content:"";
  position:absolute;
  top:-70%;
  left:-55%;
  width:34%;
  height:240%;

  background:
    linear-gradient(
      110deg,
      transparent,
      rgba(255,255,255,.10),
      rgba(255,255,255,.62),
      rgba(255,255,255,.10),
      transparent
    );

  transform:rotate(17deg);
  animation:raffleSoftPinkShine 5.8s linear infinite;
  pointer-events:none;
  z-index:2;
}

@keyframes raffleSoftPinkShine{
  0%{
    left:-55%;
  }

  45%{
    left:135%;
  }

  100%{
    left:135%;
  }
}

.raffle-header,
.raffle-names{
  position:relative;
  z-index:7;
}

.raffle-header{
  text-align:center;
  margin-bottom:12px;
}

.raffle-badge{
  display:inline-block;
  padding:4px 14px;
  border-radius:7px;

  background:
    linear-gradient(
      180deg,
      rgba(255,255,255,.62),
      rgba(255,255,255,.12) 43%,
      rgba(151,26,70,.15)
    ),
    linear-gradient(
      90deg,
      #ffd9e6,
      #ff98b9,
      #dc4678
    );

  font-family:'Bebas Neue', sans-serif;
  font-size:15px;
  line-height:1;
  letter-spacing:.08em;
  color:#ffffff;
  text-transform:uppercase;

  border:2px solid #111111;

  box-shadow:
    0 0 0 1px rgba(255,255,255,.62),
    0 0 10px rgba(255,58,124,.38),
    inset 0 2px 0 rgba(255,255,255,.75);

  -webkit-text-stroke:.7px var(--pink-dark);

  text-shadow:
    -1px -1px 0 var(--pink-dark),
     1px -1px 0 var(--pink-dark),
    -1px  1px 0 var(--pink-dark),
     1px  1px 0 var(--pink-dark),
     0 0 4px rgba(255,255,255,.86),
     2px 3px 0 #111111;
}

.raffle-title{
  margin:8px 0 0;

  font-family:'Bebas Neue', sans-serif;
  font-size:31px;
  line-height:1;
  letter-spacing:.06em;
  color:#ffffff;
  text-transform:uppercase;

  -webkit-text-stroke:1px var(--pink-dark);

  text-shadow:
    -2px -2px 0 var(--pink-dark),
     2px -2px 0 var(--pink-dark),
    -2px  2px 0 var(--pink-dark),
     2px  2px 0 var(--pink-dark),
     0 0 4px #ffffff,
     0 0 9px rgba(255,255,255,.86),
     0 0 13px rgba(255,55,123,.66),
     3px 4px 0 #111111;
}

.raffle-names{
  display:grid;
  grid-template-columns:1fr;
  gap:8px;
  width:100%;
  overflow:visible;
}

.raffle-names .raffle-name-item:nth-child(n + 6){
  display:none;
}

.raffle-name-item{
  position:relative;
  display:flex;
  justify-content:space-between;
  align-items:center;
  gap:10px;

  min-height:50px;
  padding:8px 11px 8px 17px;
  border-radius:10px;
  overflow:hidden;

  background:
    linear-gradient(
      180deg,
      rgba(255,255,255,.58),
      rgba(255,255,255,.10) 43%,
      rgba(142,24,66,.14)
    ),
    linear-gradient(
      100deg,
      #ffdce8 0%,
      #ff9cbd 52%,
      #dc4678 100%
    );

  border:2px solid #111111;

  box-shadow:
    0 0 0 1px rgba(255,255,255,.54),
    inset 0 2px 0 rgba(255,255,255,.70),
    inset 0 -8px 18px rgba(139,21,63,.16),
    0 5px 14px rgba(85,7,35,.26);
}

.raffle-name-item::before{
  content:"";
  position:absolute;
  left:0;
  top:0;
  bottom:0;
  width:7px;

  background:
    linear-gradient(
      180deg,
      #ffffff,
      #ffacc8,
      #d43e72
    );

  box-shadow:
    0 0 8px rgba(255,255,255,.70);

  z-index:2;
}

.raffle-name-item::after{
  content:"";
  position:absolute;
  top:0;
  left:-90%;
  width:50%;
  height:100%;

  background:
    linear-gradient(
      105deg,
      transparent,
      rgba(255,255,255,.14),
      rgba(255,255,255,.62),
      transparent
    );

  transform:skewX(-18deg);
  animation:raffleRowSoftPinkShine 5.1s ease-in-out infinite;
  pointer-events:none;
  z-index:1;
}

@keyframes raffleRowSoftPinkShine{
  0%{
    left:-90%;
  }

  42%{
    left:135%;
  }

  100%{
    left:135%;
  }
}

.raffle-name{
  position:relative;
  z-index:3;
  flex:1 1 auto;
  min-width:0;

  font-size:19px;
  font-weight:800;
  line-height:1;
  color:#ffffff;

  white-space:normal;
  overflow-wrap:anywhere;
  word-break:break-word;

  -webkit-text-stroke:.7px var(--pink-dark);

  text-shadow:
    -1px -1px 0 var(--pink-dark),
     1px -1px 0 var(--pink-dark),
    -1px  1px 0 var(--pink-dark),
     1px  1px 0 var(--pink-dark),
     0 0 4px rgba(255,255,255,.90),
     2px 3px 0 #111111;
}

.raffle-name-item:nth-child(n + 4) .raffle-name{
  font-size:17px;
}

.raffle-tickets{
  position:relative;
  z-index:3;
  flex:0 0 auto;

  font-family:'Bebas Neue', sans-serif;
  font-size:15px;
  line-height:1;
  letter-spacing:.04em;
  color:#ffffff;
  white-space:nowrap;

  padding:4px 9px;
  border-radius:6px;

  background:
    linear-gradient(
      180deg,
      rgba(255,255,255,.58),
      rgba(255,255,255,.10)
    ),
    linear-gradient(
      90deg,
      #ffcddd,
      #ff91b3,
      #d83f73
    );

  border:2px solid #111111;

  box-shadow:
    0 0 0 1px rgba(255,255,255,.54),
    0 0 8px rgba(255,57,123,.34);

  -webkit-text-stroke:.7px var(--pink-dark);

  text-shadow:
    -1px -1px 0 var(--pink-dark),
     1px -1px 0 var(--pink-dark),
    -1px  1px 0 var(--pink-dark),
     1px  1px 0 var(--pink-dark),
     0 0 4px rgba(255,255,255,.85),
     2px 3px 0 #111111;
}

.raffle-winner-screen{
  position:absolute;
  inset:0;
  z-index:20;

  display:flex;
  flex-direction:column;
  justify-content:center;
  align-items:center;

  padding:24px;

  background:
    radial-gradient(
      circle at center,
      rgba(255,191,214,.92),
      rgba(255,103,157,.80) 42%,
      rgba(95,14,45,.94) 82%
    );

  opacity:0;
  transform:scale(.90);

  transition:
    opacity .25s ease,
    transform .25s ease;
}

.raffle-winner-screen.show{
  opacity:1;
  transform:scale(1);
}

.raffle-winner-screen.hidden{
  opacity:0;
  pointer-events:none;
}

.winner-label,
.winner-name{
  position:relative;
  z-index:3;
  color:#ffffff;
  text-align:center;

  -webkit-text-stroke:1.2px var(--pink-dark);

  text-shadow:
    -3px -3px 0 var(--pink-dark),
     3px -3px 0 var(--pink-dark),
    -3px  3px 0 var(--pink-dark),
     3px  3px 0 var(--pink-dark),
     0 0 5px #ffffff,
     0 0 12px rgba(255,255,255,.88),
     0 0 18px rgba(255,47,117,.76),
     4px 5px 0 #111111;
}

.winner-label{
  font-family:'Bebas Neue', sans-serif;
  font-size:31px;
  line-height:1;
  letter-spacing:.08em;
  margin-bottom:10px;
  text-transform:uppercase;
}

.winner-name{
  max-width:96%;

  font-family:'Bebas Neue', sans-serif;
  font-size:53px;
  line-height:1.05;
  letter-spacing:.04em;
  text-transform:uppercase;

  overflow-wrap:anywhere;
  word-break:break-word;

  animation:winnerSoftPinkPop .7s ease-in-out infinite alternate;
}

@keyframes winnerSoftPinkPop{
  from{
    transform:scale(1);
    filter:brightness(1);
  }

  to{
    transform:scale(1.06);

    filter:
      brightness(1.14)
      drop-shadow(0 0 13px rgba(255,255,255,.80))
      drop-shadow(0 0 17px rgba(255,40,112,.72));
  }
}






