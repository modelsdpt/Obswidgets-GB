
/* ================================
   RAFFLE – Red Heartbeat Theme
================================ */
@import url('https://fonts.googleapis.com/css2?family=Bungee&family=Baloo+2:wght@400;600;700;800&display=swap');

:root{
  --txt:#ffffff;
  --outline:#ff1e1e;
  --shadow:#2b0000;
}

*{ box-sizing:border-box; }

body{
  margin:0;
  background:transparent;
  font-family:'Baloo 2', system-ui, sans-serif;
  overflow:hidden;
}

.raffle-widget{
  position:relative;
  width:560px;
  min-height:430px;
  padding:14px 16px 16px;
  border-radius:18px;
  overflow:hidden;

  background:
    radial-gradient(circle at 22% 35%, rgba(255,0,0,.36), transparent 24%),
    radial-gradient(circle at 78% 68%, rgba(120,0,0,.36), transparent 30%),
    linear-gradient(135deg, #120000 0%, #330000 28%, #850606 58%, #180000 100%);
  border:1px solid rgba(255,45,45,.72);
  box-shadow:
    0 0 18px rgba(255,0,0,.42),
    0 10px 28px rgba(0,0,0,.42),
    inset 0 0 24px rgba(255,0,0,.22);
}

.raffle-widget.hidden{
  opacity:0;
  transform:translateY(10px) scale(.96);
  pointer-events:none;
}

.raffle-widget:not(.hidden){
  opacity:1;
  transform:translateY(0) scale(1);
}

.raffle-widget::before{
  content:"";
  position:absolute;
  left:-8%;
  right:-8%;
  top:40%;
  height:34px;
  transform:translateY(-50%);
  background:#ff2020;
  clip-path:polygon(
    0 50%, 6% 50%, 9% 22%, 12% 75%, 16% 50%,
    25% 50%, 28% 12%, 31% 88%, 35% 50%,
    45% 50%, 48% 24%, 51% 78%, 55% 50%,
    65% 50%, 68% 10%, 71% 90%, 75% 50%,
    84% 50%, 87% 30%, 90% 72%, 94% 50%, 100% 50%
  );
  filter:
    drop-shadow(0 0 4px rgba(255,255,255,.72))
    drop-shadow(0 0 10px rgba(255,0,0,.95));
  opacity:.78;
  pointer-events:none;
  z-index:1;
}

.raffle-widget::after{
  content:"";
  position:absolute;
  inset:-45%;
  background:linear-gradient(
    120deg,
    transparent 0%,
    rgba(255,255,255,.05) 38%,
    rgba(255,255,255,.28) 50%,
    rgba(255,255,255,.05) 62%,
    transparent 100%
  );
  transform:translateX(-120%) rotate(12deg);
  animation:raffleRedHeartbeatShine 4.8s linear infinite;
  pointer-events:none;
  z-index:2;
  mix-blend-mode:screen;
}

@keyframes raffleRedHeartbeatShine{
  0%{ transform:translateX(-120%) rotate(12deg); }
  45%{ transform:translateX(120%) rotate(12deg); }
  100%{ transform:translateX(120%) rotate(12deg); }
}

.raffle-header,
.raffle-names{
  position:relative;
  z-index:5;
}

.raffle-header{
  text-align:center;
  margin-bottom:12px;
}

.raffle-badge{
  display:inline-block;
  padding:3px 12px;
  border-radius:999px;
  background:linear-gradient(90deg, #260000, #9b0505, #ff1f1f);
  font-family:'Bungee', system-ui, sans-serif;
  font-size:13px;
  letter-spacing:1px;
  color:#fff;
  border:1px solid rgba(255,55,55,.66);
  box-shadow:0 0 12px rgba(255,0,0,.35);
  text-shadow:
    -1px -1px 0 var(--outline),
     1px -1px 0 var(--outline),
    -1px  1px 0 var(--outline),
     1px  1px 0 var(--outline),
     0 0 8px rgba(255,0,0,.9),
     3px  3px 0 var(--shadow);
}

.raffle-title{
  margin:8px 0 0;
  font-family:'Bungee', system-ui, sans-serif;
  font-size:21px;
  line-height:1.1;
  color:#fff;
  text-shadow:
    -1px -1px 0 var(--outline),
     1px -1px 0 var(--outline),
    -1px  1px 0 var(--outline),
     1px  1px 0 var(--outline),
     0 0 8px rgba(255,0,0,.9),
     3px  3px 0 var(--shadow);
}

.raffle-names{
  display:grid;
  grid-template-columns:1fr;
  gap:7px;
  width:100%;
  overflow:visible;
}

.raffle-names .raffle-name-item:nth-child(n + 6){
  display:none;
}

.raffle-name-item{
  position:relative;
  display:flex;
  justify-content:space-between;
  align-items:center;
  gap:10px;
  min-height:48px;
  padding:7px 10px;
  border-radius:12px;
  overflow:hidden;

  background:
    linear-gradient(180deg, rgba(255,255,255,.10), rgba(255,255,255,.02)),
    linear-gradient(135deg, rgba(45,0,0,.94), rgba(150,0,0,.64), rgba(22,0,0,.96));
  border:1px solid rgba(255,55,55,.58);
  box-shadow:
    inset 0 1px 0 rgba(255,255,255,.16),
    0 0 12px rgba(255,0,0,.22);
}

.raffle-name-item::before{
  content:"";
  position:absolute;
  left:-12%;
  right:-12%;
  top:50%;
  height:22px;
  transform:translateY(-50%);
  background:#ff2020;
  clip-path:polygon(
    0 50%, 12% 50%, 15% 20%, 18% 80%, 22% 50%,
    37% 50%, 40% 16%, 43% 86%, 47% 50%,
    60% 50%, 63% 28%, 66% 74%, 70% 50%,
    84% 50%, 87% 18%, 90% 82%, 94% 50%, 100% 50%
  );
  filter:drop-shadow(0 0 7px rgba(255,0,0,.9));
  opacity:.36;
  pointer-events:none;
  z-index:1;
}

.raffle-name-item::after{
  content:"♥";
  position:absolute;
  top:50%;
  left:10px;
  transform:translateY(-50%);
  font-size:15px;
  color:#fff;
  text-shadow:
    -1px -1px 0 var(--outline),
     1px -1px 0 var(--outline),
    -1px  1px 0 var(--outline),
     1px  1px 0 var(--outline),
     0 0 8px rgba(255,0,0,.9);
  z-index:2;
}

.raffle-name{
  position:relative;
  z-index:3;
  flex:1 1 auto;
  min-width:0;
  padding-left:18px;
  font-size:18px;
  font-weight:800;
  line-height:1.05;
  color:#fff;
  white-space:normal;
  overflow-wrap:anywhere;
  word-break:break-word;
  text-shadow:
    -1px -1px 0 var(--outline),
     1px -1px 0 var(--outline),
    -1px  1px 0 var(--outline),
     1px  1px 0 var(--outline),
     0 0 8px rgba(255,0,0,.9),
     3px  3px 0 var(--shadow);
}

.raffle-name-item:nth-child(n + 4) .raffle-name{
  font-size:16px;
}

.raffle-tickets{
  position:relative;
  z-index:3;
  flex:0 0 auto;
  font-size:13px;
  font-weight:800;
  color:#fff;
  white-space:nowrap;
  padding:3px 8px;
  border-radius:999px;
  background:rgba(35,0,0,.72);
  border:1px solid rgba(255,55,55,.58);
  box-shadow:0 0 10px rgba(255,0,0,.25);
  text-shadow:
    -1px -1px 0 var(--outline),
     1px -1px 0 var(--outline),
    -1px  1px 0 var(--outline),
     1px  1px 0 var(--outline),
     0 0 8px rgba(255,0,0,.85),
     2px  2px 0 var(--shadow);
}

.raffle-winner-screen{
  position:absolute;
  inset:0;
  z-index:20;
  display:flex;
  flex-direction:column;
  justify-content:center;
  align-items:center;
  padding:24px;

  background:
    radial-gradient(circle at center, rgba(255,0,0,.48), rgba(80,0,0,.55) 42%, rgba(0,0,0,.82) 82%),
    rgba(0,0,0,.82);

  opacity:0;
  transform:scale(.9);
  transition:opacity .25s ease, transform .25s ease;
}

.raffle-winner-screen.show{
  opacity:1;
  transform:scale(1);
}

.raffle-winner-screen.hidden{
  opacity:0;
  pointer-events:none;
}

.winner-label,
.winner-name{
  position:relative;
  z-index:3;
  font-family:'Bungee', system-ui, sans-serif;
  color:#fff;
  text-align:center;
  text-shadow:
    -1px -1px 0 var(--outline),
     1px -1px 0 var(--outline),
    -1px  1px 0 var(--outline),
     1px  1px 0 var(--outline),
     0 0 10px rgba(255,0,0,.95),
     3px  3px 0 var(--shadow);
}

.winner-label{
  font-size:30px;
  line-height:1;
  margin-bottom:10px;
}

.winner-name{
  max-width:96%;
  font-size:46px;
  line-height:1.08;
  overflow-wrap:anywhere;
  word-break:break-word;
  animation:winnerRedHeartbeatPop .7s ease infinite alternate;
}

@keyframes winnerRedHeartbeatPop{
  from{ transform:scale(1); }
  to{
    transform:scale(1.06);
    filter:drop-shadow(0 0 10px rgba(255,0,0,.75));
  }
}