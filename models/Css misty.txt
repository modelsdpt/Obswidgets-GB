/* =========================
   MVP – Black Grey Outline Theme
   ========================= */
@import url('https://fonts.googleapis.com/css2?family=Orbitron:wght@700;800&family=Montserrat:wght@700;800&display=swap');

:root{
  --txt:#ffffff;
  --outline:#bfc3cc;
  --shadow:#050505;

  --gold-1:#ffe9a6;
  --gold-2:#ffd86b;
  --gold-3:#ffbf2f;

  --silver-1:#f6f7fb;
  --silver-2:#d9deea;
  --silver-3:#b8c0cf;

  --bronze-1:#f2c6a2;
  --bronze-2:#d99a67;
  --bronze-3:#b86e36;
}

*{ box-sizing:border-box; }

body{
  margin:0;
  background:transparent;
  color:var(--txt);
  font-family:'Montserrat', system-ui, sans-serif;
}

#container{
  position:relative;
  width:430px;
  padding:12px 14px 14px;
  border-radius:20px;
  overflow:hidden;

  background:
    radial-gradient(circle at 14% 18%, rgba(255,255,255,.12), transparent 18%),
    radial-gradient(circle at 82% 20%, rgba(255,255,255,.08), transparent 20%),
    linear-gradient(145deg, #1d1e24 0%, #0b0c10 44%, #020203 100%);
  border:1px solid rgba(255,255,255,.18);
  box-shadow:
    0 12px 30px rgba(0,0,0,.70),
    0 0 18px rgba(210,215,225,.10),
    inset 0 1px 0 rgba(255,255,255,.08);
}

#container::before{
  content:"";
  position:absolute;
  inset:0;
  border-radius:inherit;
  padding:1px;
  background:
    linear-gradient(130deg, rgba(255,255,255,.40), rgba(180,185,195,.20), rgba(255,255,255,.14), rgba(210,215,225,.30));
  -webkit-mask:linear-gradient(#000 0 0) content-box, linear-gradient(#000 0 0);
  -webkit-mask-composite:xor;
          mask-composite:exclude;
  pointer-events:none;
  z-index:4;
}

#container::after{
  content:"";
  position:absolute;
  inset:-45%;
  background:linear-gradient(
    120deg,
    transparent 0%,
    rgba(255,255,255,.06) 38%,
    rgba(255,255,255,.30) 50%,
    rgba(255,255,255,.06) 62%,
    transparent 100%
  );
  transform:translateX(-120%) rotate(12deg);
  animation:blackGreyShine 4.4s linear infinite;
  pointer-events:none;
  z-index:3;
  mix-blend-mode:screen;
}

@keyframes blackGreyShine{
  0%{ transform:translateX(-120%) rotate(12deg); }
  45%{ transform:translateX(120%) rotate(12deg); }
  100%{ transform:translateX(120%) rotate(12deg); }
}

.title{
  position:relative;
  z-index:5;
  margin:0 0 12px;
  text-align:center;
  font-family:'Orbitron', sans-serif;
  font-size:24px;
  font-weight:800;
  letter-spacing:.16em;
  text-transform:uppercase;
  color:#fff;
  text-shadow:
    -1px -1px 0 var(--outline),
     1px -1px 0 var(--outline),
    -1px  1px 0 var(--outline),
     1px  1px 0 var(--outline),
     3px  3px 0 var(--shadow),
     0 0 10px rgba(255,255,255,.18);
}

#top-list{
  position:relative;
  z-index:5;
  display:flex;
  flex-direction:column;
  gap:12px;
}

.tip{
  position:relative;
  display:flex;
  align-items:center;
  justify-content:space-between;
  gap:10px;
  padding:10px 14px;
  border-radius:14px;
  overflow:hidden;

  background:
    linear-gradient(180deg, rgba(255,255,255,.06), rgba(255,255,255,.02)),
    linear-gradient(135deg, #25262d 0%, #111217 54%, #050506 100%);
  border:1px solid rgba(255,255,255,.12);
  box-shadow:
    inset 0 1px 0 rgba(255,255,255,.08),
    0 7px 18px rgba(0,0,0,.45);
}

.tip::before{
  content:"";
  position:absolute;
  inset:-45%;
  background:linear-gradient(
    120deg,
    transparent 0%,
    rgba(255,255,255,.06) 38%,
    rgba(255,255,255,.26) 50%,
    rgba(255,255,255,.06) 62%,
    transparent 100%
  );
  transform:translateX(-120%) rotate(12deg);
  animation:blackGreyShine 4.4s linear infinite;
  pointer-events:none;
  z-index:1;
  mix-blend-mode:screen;
}

.tip::after{
  content:"✦";
  position:absolute;
  top:50%;
  left:10px;
  transform:translateY(-50%);
  font-size:14px;
  color:#fff;
  text-shadow:
    1px 1px 0 var(--shadow),
    0 0 8px rgba(255,255,255,.32);
  z-index:2;
}

.tip .name{
  position:relative;
  z-index:3;
  flex:1;
  min-width:0;
  padding-left:18px;
  font-size:17px;
  font-weight:800;
  color:#fff;
  white-space:nowrap;
  overflow:hidden;
  text-overflow:ellipsis;
}

.tip .amount{
  position:relative;
  z-index:3;
  font-family:'Orbitron', sans-serif;
  font-size:19px;
  font-weight:800;
  color:#fff;
  white-space:nowrap;
}

.tip .name,
.tip .amount{
  text-shadow:
    -1px -1px 0 var(--outline),
     1px -1px 0 var(--outline),
    -1px  1px 0 var(--outline),
     1px  1px 0 var(--outline),
     3px  3px 0 var(--shadow);
}

.tip.mvp{
  background:
    linear-gradient(180deg, rgba(255,255,255,.22), rgba(255,255,255,.05)),
    linear-gradient(135deg, var(--gold-1) 0%, var(--gold-2) 46%, var(--gold-3) 100%);
  border-color:rgba(255,236,170,.58);
}

.tip.mvp::after{ content:"👑"; }

.tip.second{
  background:
    linear-gradient(180deg, rgba(255,255,255,.20), rgba(255,255,255,.04)),
    linear-gradient(135deg, var(--silver-1) 0%, var(--silver-2) 48%, var(--silver-3) 100%);
}

.tip.third{
  background:
    linear-gradient(180deg, rgba(255,255,255,.18), rgba(255,255,255,.03)),
    linear-gradient(135deg, var(--bronze-1) 0%, var(--bronze-2) 48%, var(--bronze-3) 100%);
}

.rank-icon{
  position:relative;
  z-index:3;
  margin-right:6px;
  font-size:17px;
  color:#fff;
  filter:drop-shadow(2px 2px 0 var(--shadow));
}


GOALBAR


/* =========================
   GOAL BAR VERTICAL – Black Grey Outline Theme
   ========================= */
@import url('https://fonts.googleapis.com/css2?family=Orbitron:wght@700;800&family=Montserrat:wght@700;800&display=swap');

:root{
  --txt:#ffffff;
  --outline:#bfc3cc;
  --shadow:#050505;
}

*{ box-sizing:border-box; }

body{
  margin:0;
  background:transparent;
  font-family:'Montserrat', system-ui, sans-serif;
  color:var(--txt);
}

#goal-wrapper{
  width:65px;
  height:420px;
  position:relative;
  overflow:hidden;
  border-radius:20px;

  background:
    radial-gradient(circle at 20% 10%, rgba(255,255,255,.12), transparent 18%),
    radial-gradient(circle at 76% 16%, rgba(255,255,255,.08), transparent 16%),
    linear-gradient(180deg, #1d1e24 0%, #0b0c10 48%, #020203 100%);
  border:1px solid rgba(255,255,255,.18);
  box-shadow:
    0 10px 24px rgba(0,0,0,.65),
    0 0 18px rgba(210,215,225,.10),
    inset 0 1px 0 rgba(255,255,255,.08);
}

#goal-wrapper::before{
  content:"";
  position:absolute;
  inset:0;
  border-radius:inherit;
  padding:1px;
  background:
    linear-gradient(180deg, rgba(255,255,255,.40), rgba(180,185,195,.20), rgba(255,255,255,.10), rgba(210,215,225,.30));
  -webkit-mask:linear-gradient(#000 0 0) content-box, linear-gradient(#000 0 0);
  -webkit-mask-composite:xor;
          mask-composite:exclude;
  opacity:.96;
  pointer-events:none;
  z-index:5;
}

#goal-wrapper::after{
  content:"";
  position:absolute;
  inset:-55%;
  background:linear-gradient(
    120deg,
    transparent 0%,
    rgba(255,255,255,.07) 38%,
    rgba(255,255,255,.30) 50%,
    rgba(255,255,255,.07) 62%,
    transparent 100%
  );
  transform:translateY(120%) rotate(12deg);
  animation:goalBlackGreyShine 4.8s linear infinite;
  pointer-events:none;
  z-index:3;
  mix-blend-mode:screen;
}

@keyframes goalBlackGreyShine{
  0%{ transform:translateY(120%) rotate(12deg); }
  45%{ transform:translateY(-120%) rotate(12deg); }
  100%{ transform:translateY(-120%) rotate(12deg); }
}

#bar-fill{
  position:absolute;
  bottom:0;
  left:0;
  width:100%;
  height:0%;
  z-index:2;
  border-radius:0 0 20px 20px;

  background:
    linear-gradient(180deg, rgba(255,255,255,.22), rgba(255,255,255,.05)),
    linear-gradient(180deg, rgba(255,255,255,.34) 0%, rgba(190,195,210,.26) 50%, rgba(255,255,255,.18) 100%);
  box-shadow:
    inset 0 1px 0 rgba(255,255,255,.30),
    inset 0 0 22px rgba(255,255,255,.10),
    0 0 16px rgba(255,255,255,.18);
  overflow:hidden;
}

#bar-fill::before{
  content:"";
  position:absolute;
  inset:-20%;
  background:linear-gradient(
    180deg,
    transparent 0%,
    rgba(255,255,255,.12) 38%,
    rgba(255,255,255,.36) 50%,
    rgba(255,255,255,.12) 62%,
    transparent 100%
  );
  transform:translateY(120%);
  animation:goalFillBlackGreyShine 4.6s linear infinite;
  mix-blend-mode:screen;
  opacity:.86;
}

#bar-fill::after{
  content:"✦  •  ✦";
  position:absolute;
  left:0;
  right:0;
  bottom:8px;
  text-align:center;
  font-size:10px;
  line-height:1.8;
  color:rgba(255,255,255,.78);
  text-shadow:
    1px 1px 0 var(--shadow),
    0 0 8px rgba(255,255,255,.32);
  animation:greyParticles 3.8s linear infinite;
  pointer-events:none;
  opacity:.90;
}

@keyframes goalFillBlackGreyShine{
  0%{ transform:translateY(120%); }
  45%{ transform:translateY(-120%); }
  100%{ transform:translateY(-120%); }
}

@keyframes greyParticles{
  0%{ transform:translateY(12px); opacity:.18; }
  20%{ opacity:.90; }
  100%{ transform:translateY(-110px); opacity:0; }
}

.goal-label{
  position:absolute;
  inset:0;
  display:flex;
  justify-content:center;
  align-items:center;
  pointer-events:none;
  z-index:6;
}

.goal-title{
  position:absolute;
  top:50%;
  left:50%;
  transform:translate(-50%, -50%) rotate(-90deg);
  transform-origin:center;

  font-family:'Orbitron', sans-serif;
  font-size:14px;
  font-weight:800;
  letter-spacing:.12em;
  text-transform:uppercase;
  color:#fff;
  white-space:nowrap;

  text-shadow:
    -1px -1px 0 var(--outline),
     1px -1px 0 var(--outline),
    -1px  1px 0 var(--outline),
     1px  1px 0 var(--outline),
     3px  3px 0 var(--shadow);
}

.goal-amount{
  position:absolute;
  bottom:42px;
  left:50%;
  transform:translateX(-50%) rotate(-90deg);
  transform-origin:center;

  font-family:'Orbitron', sans-serif;
  font-size:12px;
  font-weight:800;
  color:#fff;
  white-space:nowrap;
  padding:2px 6px;
  border-radius:8px;
  background:rgba(0,0,0,.30);

  text-shadow:
    -1px -1px 0 var(--outline),
     1px -1px 0 var(--outline),
    -1px  1px 0 var(--outline),
     1px  1px 0 var(--outline),
     3px  3px 0 var(--shadow);
}

.goal-title.neon{
  animation:goalPulseBlackGrey 1.25s ease-in-out infinite alternate;
}

@keyframes goalPulseBlackGrey{
  from{ filter:brightness(1); }
  to{ filter:brightness(1.12); }
}


TIMER

/* =========================
   TIMER – Black Grey Outline Theme
   ========================= */
@import url('https://fonts.googleapis.com/css2?family=Anton&family=Montserrat:wght@700;800&display=swap');

:root{
  --txt:#ffffff;
  --outline:#bfc3cc;
  --shadow:#050505;
}

*{ box-sizing:border-box; }

body{
  margin:0;
  background:transparent;
  font-family:'Montserrat', system-ui, sans-serif;
  color:var(--txt);
}

.timer-widget{
  position:relative;
  width:280px;
  padding:14px 14px 16px;
  border-radius:18px;
  overflow:hidden;

  background:
    radial-gradient(circle at 14% 18%, rgba(255,255,255,.12), transparent 20%),
    radial-gradient(circle at 82% 22%, rgba(255,255,255,.08), transparent 24%),
    linear-gradient(145deg, #1d1e24 0%, #0b0c10 48%, #020203 100%);
  border:1px solid rgba(255,255,255,.18);
  box-shadow:
    0 8px 22px rgba(0,0,0,.62),
    0 0 16px rgba(210,215,225,.10),
    inset 0 1px 0 rgba(255,255,255,.08);
}

.timer-widget::after{
  content:"";
  position:absolute;
  inset:-45%;
  background:linear-gradient(
    120deg,
    transparent 0%,
    rgba(255,255,255,.07) 38%,
    rgba(255,255,255,.30) 50%,
    rgba(255,255,255,.07) 62%,
    transparent 100%
  );
  transform:translateX(-120%) rotate(12deg);
  animation:timerBlackGreyShine 4.8s linear infinite;
  pointer-events:none;
  z-index:2;
  mix-blend-mode:screen;
}

@keyframes timerBlackGreyShine{
  0%{ transform:translateX(-120%) rotate(12deg); }
  45%{ transform:translateX(120%) rotate(12deg); }
  100%{ transform:translateX(120%) rotate(12deg); }
}

.timer-title{
  position:relative;
  z-index:3;
  text-align:center;
  font-family:'Anton', sans-serif;
  font-size:18px;
  letter-spacing:.06em;
  text-transform:uppercase;
  color:#fff;
  margin-bottom:10px;
  text-shadow:
    -1px -1px 0 var(--outline),
     1px -1px 0 var(--outline),
    -1px  1px 0 var(--outline),
     1px  1px 0 var(--outline),
     3px  3px 0 var(--shadow);
}

.timer-display{
  position:relative;
  z-index:3;
  display:flex;
  justify-content:center;
}

.timer-box{
  position:relative;
  width:170px;
  height:72px;
  border-radius:8px;
  overflow:hidden;
  display:flex;
  align-items:center;
  justify-content:center;

  background:
    radial-gradient(circle at 18% 40%, rgba(255,255,255,.12), transparent 15%),
    radial-gradient(circle at 70% 38%, rgba(255,255,255,.08), transparent 14%),
    linear-gradient(135deg, rgba(255,255,255,.10) 0%, rgba(60,62,75,.46) 45%, rgba(6,6,8,.86) 100%);
  border:1px solid rgba(255,255,255,.14);
  box-shadow:
    0 4px 12px rgba(0,0,0,.44),
    inset 0 1px 0 rgba(255,255,255,.12);
}

.timer-box::before{
  content:"";
  position:absolute;
  inset:0;
  background:
    linear-gradient(180deg, rgba(255,255,255,.08), transparent 38%),
    repeating-linear-gradient(
      115deg,
      rgba(255,255,255,.018) 0px,
      rgba(255,255,255,.018) 2px,
      rgba(255,255,255,0) 2px,
      rgba(255,255,255,0) 8px
    );
  opacity:.86;
}

.timer-box::after{
  content:"";
  position:absolute;
  inset:-30%;
  background:linear-gradient(
    120deg,
    transparent 0%,
    rgba(255,255,255,.08) 35%,
    rgba(255,255,255,.24) 50%,
    rgba(255,255,255,.08) 62%,
    transparent 78%
  );
  transform:translateX(-120%);
  animation:timerBoxBlackGreyShine 4.4s linear infinite;
  mix-blend-mode:screen;
  opacity:.86;
}

@keyframes timerBoxBlackGreyShine{
  0%{ transform:translateX(-120%); }
  40%{ transform:translateX(120%); }
  100%{ transform:translateX(120%); }
}

#timer-value{
  position:relative;
  z-index:1;
  font-family:'Anton', sans-serif;
  font-size:34px;
  letter-spacing:.06em;
  color:#fff;
  text-shadow:
    -1px -1px 0 var(--outline),
     1px -1px 0 var(--outline),
    -1px  1px 0 var(--outline),
     1px  1px 0 var(--outline),
     3px  3px 0 var(--shadow);
}

.timer-box.timer-running{
  animation:timerPulseBlackGrey 1.15s ease-in-out infinite alternate;
}

@keyframes timerPulseBlackGrey{
  from{ transform:translateY(0); }
  to{
    transform:translateY(-1px);
    box-shadow:
      0 6px 16px rgba(0,0,0,.52),
      inset 0 1px 0 rgba(255,255,255,.16),
      0 0 16px rgba(210,215,225,.12);
  }
}

.timer-box.timer-finished{
  box-shadow:
    0 6px 16px rgba(0,0,0,.52),
    inset 0 1px 0 rgba(255,255,255,.16),
    0 0 20px rgba(210,215,225,.14);
}



Raffle



/* ================================
   RAFFLE – Black Grey Outline Theme
================================ */
@import url('https://fonts.googleapis.com/css2?family=Bungee&family=Baloo+2:wght@400;600;700;800&display=swap');

:root {
  --raffle-main: #1d1e24;
  --raffle-strong: #050505;
  --raffle-soft: #d7dbe4;
  --raffle-light: #ffffff;
  --raffle-gold: #ffd966;

  --raffle-border: rgba(255, 255, 255, 0.18);
  --raffle-glow: rgba(210, 215, 225, 0.20);

  --txt: #ffffff;
  --outline: #bfc3cc;
  --shadow: #050505;
}

* {
  box-sizing: border-box;
}

body {
  margin: 0;
  background: transparent;
  font-family: 'Baloo 2', system-ui, sans-serif;
  color: var(--txt);
  overflow: hidden;
}

.raffle-widget {
  position: relative;
  width: 560px;
  min-height: 430px;
  padding: 14px 16px 16px;
  border-radius: 18px;
  overflow: hidden;

  background:
    radial-gradient(circle at 14% 18%, rgba(255,255,255,.12), transparent 18%),
    radial-gradient(circle at 80% 20%, rgba(255,255,255,.08), transparent 22%),
    linear-gradient(145deg, #1d1e24 0%, #0b0c10 48%, #020203 100%);

  border: 1px solid var(--raffle-border);

  box-shadow:
    0 12px 30px rgba(0,0,0,.70),
    0 0 18px rgba(210,215,225,.10),
    inset 0 1px 0 rgba(255,255,255,.08);

  transition:
    opacity 0.25s ease,
    transform 0.25s ease;
}

.raffle-widget.hidden {
  opacity: 0;
  transform: translateY(10px) scale(0.96);
  pointer-events: none;
}

.raffle-widget:not(.hidden) {
  opacity: 1;
  transform: translateY(0) scale(1);
}

.raffle-widget::before {
  content: "";
  position: absolute;
  inset: 0;
  border-radius: inherit;
  padding: 1px;

  background:
    linear-gradient(
      130deg,
      rgba(255,255,255,.40),
      rgba(180,185,195,.20),
      rgba(255,255,255,.12),
      rgba(210,215,225,.30)
    );

  -webkit-mask:
    linear-gradient(#000 0 0) content-box,
    linear-gradient(#000 0 0);
  -webkit-mask-composite: xor;
          mask-composite: exclude;

  pointer-events: none;
  z-index: 3;
}

.raffle-widget::after {
  content: "✦   ✦   ✦   ✦";
  position: absolute;
  inset: 0;
  display: flex;
  justify-content: center;
  align-items: flex-start;
  padding-top: 8px;
  font-size: 13px;
  letter-spacing: 13px;
  color: rgba(255,255,255,.18);
  text-shadow: 0 0 8px rgba(255,255,255,.18);
  pointer-events: none;
  z-index: 1;
}

.raffle-header {
  position: relative;
  z-index: 5;
  text-align: center;
  margin-bottom: 12px;
}

.raffle-header::before {
  content: "";
  position: absolute;
  inset: -260px -260px;

  background: linear-gradient(
    120deg,
    transparent 0%,
    rgba(255,255,255,.07) 38%,
    rgba(255,255,255,.30) 50%,
    rgba(255,255,255,.07) 62%,
    transparent 100%
  );

  transform: translateX(-120%) rotate(12deg);
  animation: raffleBlackGreyShine 4.8s linear infinite;
  pointer-events: none;
  z-index: -1;
  mix-blend-mode: screen;
}

@keyframes raffleBlackGreyShine {
  0% { transform: translateX(-120%) rotate(12deg); }
  45% { transform: translateX(120%) rotate(12deg); }
  100% { transform: translateX(120%) rotate(12deg); }
}

.raffle-badge {
  display: inline-block;
  padding: 3px 12px;
  border-radius: 999px;

  background:
    linear-gradient(90deg, #2a2b33, #111217, #050506);

  font-family: 'Bungee', system-ui, sans-serif;
  font-size: 13px;
  letter-spacing: 1px;
  color: #ffffff;
  border: 1px solid rgba(255,255,255,.20);

  text-shadow:
    -1px -1px 0 var(--outline),
     1px -1px 0 var(--outline),
    -1px  1px 0 var(--outline),
     1px  1px 0 var(--outline),
     3px  3px 0 var(--shadow);

  box-shadow:
    0 0 14px rgba(210,215,225,.12),
    inset 0 1px 0 rgba(255,255,255,.12);
}

.raffle-title {
  margin: 8px 0 0;
  font-family: 'Bungee', system-ui, sans-serif;
  font-size: 21px;
  line-height: 1.1;
  color: #ffffff;

  text-shadow:
    -1px -1px 0 var(--outline),
     1px -1px 0 var(--outline),
    -1px  1px 0 var(--outline),
     1px  1px 0 var(--outline),
     3px  3px 0 var(--shadow);
}

.raffle-names {
  position: relative;
  z-index: 5;
  display: grid;
  grid-template-columns: 1fr;
  gap: 7px;
  width: 100%;
  overflow: visible;
}

.raffle-names .raffle-name-item:nth-child(n + 6) {
  display: none;
}

.raffle-name-item {
  position: relative;
  display: flex;
  justify-content: space-between;
  align-items: center;
  gap: 10px;

  min-height: 48px;
  padding: 7px 10px;
  border-radius: 12px;
  overflow: hidden;

  background:
    radial-gradient(circle at 18% 30%, rgba(255,255,255,.10), transparent 14%),
    linear-gradient(180deg, rgba(255,255,255,.06), rgba(255,255,255,.02)),
    linear-gradient(135deg, #25262d 0%, #111217 54%, #050506 100%);

  border: 1px solid rgba(255,255,255,.12);

  box-shadow:
    inset 0 1px 0 rgba(255,255,255,.08),
    0 7px 18px rgba(0,0,0,.45);
}

.raffle-name-item::before {
  content: "";
  position: absolute;
  inset: -45%;
  background: linear-gradient(
    120deg,
    transparent 0%,
    rgba(255,255,255,.06) 38%,
    rgba(255,255,255,.26) 50%,
    rgba(255,255,255,.06) 62%,
    transparent 100%
  );
  transform: translateX(-120%) rotate(12deg);
  animation: raffleNameBlackGreyShine 4.4s linear infinite;
  pointer-events: none;
  z-index: 1;
  mix-blend-mode: screen;
}

@keyframes raffleNameBlackGreyShine {
  0% { transform: translateX(-120%) rotate(12deg); }
  45% { transform: translateX(120%) rotate(12deg); }
  100% { transform: translateX(120%) rotate(12deg); }
}

.raffle-name-item::after {
  content: "✦";
  position: absolute;
  top: 50%;
  left: 10px;
  transform: translateY(-50%);
  font-size: 14px;
  color: #ffffff;
  text-shadow:
    1px 1px 0 var(--shadow),
    0 0 8px rgba(255,255,255,.24);
  z-index: 2;
  pointer-events: none;
}

.raffle-name-item.active {
  transform: scale(1.025);
  border-color: #ffffff;

  background:
    radial-gradient(circle at 18% 30%, rgba(255,255,255,.14), transparent 14%),
    linear-gradient(90deg, rgba(255,255,255,.14), rgba(180,185,195,.20));

  box-shadow:
    inset 0 1px 0 rgba(255,255,255,.18),
    0 0 18px rgba(255,255,255,.24),
    0 0 24px rgba(210,215,225,.14);
}

.raffle-name {
  position: relative;
  z-index: 3;
  flex: 1 1 auto;
  min-width: 0;
  padding-left: 18px;

  font-size: 18px;
  font-weight: 800;
  line-height: 1.05;
  color: #ffffff;

  text-shadow:
    -1px -1px 0 var(--outline),
     1px -1px 0 var(--outline),
    -1px  1px 0 var(--outline),
     1px  1px 0 var(--outline),
     3px  3px 0 var(--shadow);

  white-space: normal;
  overflow-wrap: anywhere;
  word-break: break-word;
}

.raffle-name-item:nth-child(n + 4) .raffle-name {
  font-size: 16px;
}

.raffle-tickets {
  position: relative;
  z-index: 3;
  flex: 0 0 auto;

  font-size: 13px;
  font-weight: 800;
  color: #ffffff;
  white-space: nowrap;

  padding: 3px 8px;
  border-radius: 999px;

  background: rgba(0,0,0,.30);
  border: 1px solid rgba(255,255,255,.20);

  text-shadow:
    -1px -1px 0 var(--outline),
     1px -1px 0 var(--outline),
    -1px  1px 0 var(--outline),
     1px  1px 0 var(--outline),
     2px  2px 0 var(--shadow);
}

.raffle-winner-screen {
  position: absolute;
  inset: 0;
  z-index: 20;

  display: flex;
  flex-direction: column;
  justify-content: center;
  align-items: center;

  padding: 24px;

  background:
    radial-gradient(circle at center, rgba(210,215,225,.18), rgba(0,0,0,.88) 76%),
    rgba(0,0,0,.88);

  opacity: 0;
  transform: scale(0.9);

  transition:
    opacity 0.25s ease,
    transform 0.25s ease;
}

.raffle-winner-screen.show {
  opacity: 1;
  transform: scale(1);
}

.raffle-winner-screen.hidden {
  opacity: 0;
  pointer-events: none;
}

.raffle-winner-screen::after {
  content: "";
  position: absolute;
  inset: -45%;
  background: linear-gradient(
    120deg,
    transparent 0%,
    rgba(255,255,255,.07) 38%,
    rgba(255,255,255,.30) 50%,
    rgba(255,255,255,.07) 62%,
    transparent 100%
  );
  transform: translateX(-120%) rotate(12deg);
  animation: raffleWinnerBlackGreyShine 4.4s linear infinite;
  pointer-events: none;
  z-index: 1;
  mix-blend-mode: screen;
}

@keyframes raffleWinnerBlackGreyShine {
  0% { transform: translateX(-120%) rotate(12deg); }
  45% { transform: translateX(120%) rotate(12deg); }
  100% { transform: translateX(120%) rotate(12deg); }
}

.winner-label {
  position: relative;
  z-index: 3;
  font-family: 'Bungee', system-ui, sans-serif;
  font-size: 30px;
  line-height: 1;
  color: #ffffff;
  margin-bottom: 10px;

  text-shadow:
    -1px -1px 0 var(--outline),
     1px -1px 0 var(--outline),
    -1px  1px 0 var(--outline),
     1px  1px 0 var(--outline),
     3px  3px 0 var(--shadow);
}

.winner-name {
  position: relative;
  z-index: 3;
  max-width: 96%;

  font-family: 'Bungee', system-ui, sans-serif;
  font-size: 46px;
  line-height: 1.08;

  color: #ffffff;
  text-align: center;

  overflow-wrap: anywhere;
  word-break: break-word;

  text-shadow:
    -1px -1px 0 var(--outline),
     1px -1px 0 var(--outline),
    -1px  1px 0 var(--outline),
     1px  1px 0 var(--outline),
     3px  3px 0 var(--shadow);

  animation: winnerPop 0.7s ease infinite alternate;
}

@keyframes winnerPop {
  from { transform: scale(1); }
  to { transform: scale(1.06); }
}

@media (max-width: 520px) {
  .raffle-widget {
    width: 96vw;
    min-height: 420px;
    padding: 12px;
  }

  .raffle-title { font-size: 18px; }

  .raffle-name-item {
    min-height: 46px;
    padding: 7px 9px;
  }

  .raffle-name { font-size: 16px; }

  .raffle-name-item:nth-child(n + 4) .raffle-name {
    font-size: 15px;
  }

  .raffle-tickets {
    font-size: 12px;
    padding: 2px 7px;
  }

  .winner-label { font-size: 25px; }

  .winner-name { font-size: 36px; }
}